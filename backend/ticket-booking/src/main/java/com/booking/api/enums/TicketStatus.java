package com.booking.api.enums;

/**
 * Trạng thái của Vé (Ticket)
 */
public enum TicketStatus {
    ACTIVE,     // Vé đang có hiệu lực
    CANCELLED,  // Vé đã bị hủy
    USED        // Vé đã được sử dụng
}
